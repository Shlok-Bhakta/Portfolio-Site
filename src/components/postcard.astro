---
import "../styles/global.css";

const { image, tag, color } = Astro.props;


---

<div>
    <div class="flex flex-row items-center space-x-2 bg-surface0 rounded-full" >
        <img class="pl-2"src={image}>
        <div class="pr-2 text-sm text-text">{tag}</div>
    </div>
</div>