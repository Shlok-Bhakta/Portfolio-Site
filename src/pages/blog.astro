---
import Navbar from "../components/navbar.astro";
import "../styles/global.css";
import PostCard from "../components/postcard.astro"
import PocketBase from 'pocketbase';
const pb = new PocketBase('http://db.shlokbhakta.dev');
let data = await pb.collection('Tags').getFullList({
    sort: '-created',
});

http://pocketbase:8090/api/files/COLLECTION_ID_OR_NAME/RECORD_ID/FILENAME
for(let i = 0; i < data.length; i++){
  data[i].imgurl = pb.files.getUrl(data[i], data[i].Icon);
}

console.log(data)
---

<!doctype html>
<html lang="en">
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta charset="UTF-8" />
    <meta name="author" content="Shlok Bhakta" />
    <meta
      name="keywords"
      content="HTML, CSS, JavaScript, Hire, Blog, Personal Site, "
    />
    <meta
      name="description"
      content="This is Shlok Bhakta. I am a developer who is studying at Texas A&M University who is adept at many things all the way from CI/CD to backend code to web dev. Come check out what I do!"
    />
  </head>
  <body class="mocha bg-crust">
    <Navbar />
    <section
      class="flex justify-center bg-mantle w-vw h-svh m-10 rounded-md"
      style="height: 500vh;"
    >
      <div
        class="block bg-gradient-to-r from-green to-sapphire text-transparent bg-clip-text text-[100%] max-w-96 nerdfont"
      >
        read more bruh
      </div>
      <ul>
        {data.map((item) => (
          <li>
            <PostCard image={item.imgurl} tag={item.tagName} color={item.color}></PostCard>
          </li>
        ))}
      </ul>
    </section>
  </body>
</html>
